# raw Gemini output — the shipped covers are the WebP in assets/covers
tools/.covers-raw/

# local scratch
*.log
.DS_Store
Thumbs.db

# local studio auth token — never commit (public repo)
tools/.token

# raw generated scenes — the shipped layers are in assets/scenes
tools/.scenes-raw/
